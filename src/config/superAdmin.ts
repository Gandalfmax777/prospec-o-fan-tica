/**
 * E-mail do superadmin global do sistema.
 *
 * Deve corresponder à env var SUPER_ADMIN_EMAIL do backend — só este usuário
 * enxerga o painel /sysadmin e o link de acesso a ele na navegação.
 *
 * Fonte única da verdade no frontend: importe daqui em vez de duplicar o literal.
 */
export const SUPER_ADMIN_EMAIL = "joaovitorsc@gmail.com";
